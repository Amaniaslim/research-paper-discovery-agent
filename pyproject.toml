[tool.ruff]
target-version = "py312"
line-length = 120
exclude = [
    ".git",
    ".venv",
    "build",
    "demo_output",
    "dist",
    "release",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
